# Пример дополнительного правила (example overlay)

> Dummy-файл из examples/overlay/. Реальное правило живёт в private overlay.

Дополнительное on-demand правило проекта. Устанавливается как
`<contextDir>/rules/example-rule.md` и загружается агентами по триггеру.