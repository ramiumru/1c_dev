# Стандарты разработки (пример)

> Этот файл — пример. Замените его содержимым стандартов вашей компании.

## Именование
- новые реквизиты/ТЧ в типовых объектах — с префиксом проекта (из `.dev.env`, поле `PREFIX`);
- новые объекты — с префиксом проекта; подчинённые элементы — без префикса.

## Комментарии изменений
`// ++ #<TASK-ID>` … `// -- #<TASK-ID>`. Без фамилий, дат, emoji.
